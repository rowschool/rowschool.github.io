# Row School

### Developing

`main` is the user-facing development branch.

Run `npm start` or `gatsby develop`.

### Building and Deploying

`prod` is the user-facing production branch.

Running `npm run deploy` pushes a built project to this branch.
